Frontend de protótipo: página estática para upload que aponta para a API em `http://localhost:4000/upload`.

Acesso local: http://localhost:4000/public
Acesso via domínio: configure o proxy/rewrite do domínio para apontar para http://localhost:4000 e use /public para carregar o frontend.